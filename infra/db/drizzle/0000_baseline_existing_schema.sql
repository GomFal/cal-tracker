-- Drizzle baseline for the schema created by infra/db/migrations/*.sql.
-- Existing legacy migrations remain immutable; this migration only lets
-- Drizzle own forward migration snapshots from this point on.
SELECT 1;
